#include <stdio.h>
#include <string.h>

int main(){
  // printf("%long", a);

  // char myS[20000];
  // printf("Enter a string: ");
  // scanf("%s", myS);
  // int len = strlen(myS);

  // for(int i = 0 ; i < len / 2; i++){
  //   char temp = myS[i];
  //   myS[i] = myS[len - i - 1]; 
  //   myS[len - i - 1] = temp;
  // }
  // printf("%s", myS);
  return 0;
}